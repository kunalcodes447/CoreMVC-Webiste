# CoreMVC-Webiste
A mini web application built using Spring Core MVC architecture. It demonstrates handling HTTP requests, returning dynamic views using ModelAndView, and applying the MVC pattern for clear separation of concerns between controllers, models, and views.
